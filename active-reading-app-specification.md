# アクティブリーディング管理アプリ 開発仕様書

## プロジェクト概要

DaiGo式アクティブリーディングメソッドを実践するためのスマートフォン向けWebアプリケーション。
受動的な読書を能動的な学習体験に変え、読んだ内容を確実に自分のものにするための学習支援ツール。

---

## 技術スタック

- **フロントエンド**: HTML5, CSS3, JavaScript (Vanilla JS)
- **データ永続化**: LocalStorage API
- **グラフ表示**: Chart.js (v4.x)
- **レスポンシブ対応**: Mobile-first design
- **ホスティング**: Static HTML (GitHub Pages推奨)

---

## プロジェクト構成

```
active-reading-app/
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── app.js           # メインアプリケーションロジック
│   ├── storage.js       # LocalStorage操作
│   ├── book.js          # 本管理機能
│   ├── notes.js         # メモ機能
│   ├── actions.js       # 実践項目機能
│   └── stats.js         # 統計機能
└── README.md
```

---

## データ構造

### Book (本)
```javascript
{
  id: string,              // UUID
  title: string,           // 本のタイトル
  author: string,          // 著者名
  category: string,        // カテゴリー（ビジネス、自己啓発、健康など）
  status: string,          // 'before' | 'reading' | 'completed'
  startDate: string,       // ISO 8601形式 (YYYY-MM-DD)
  endDate: string | null,  // ISO 8601形式 (YYYY-MM-DD)
  createdAt: string        // ISO 8601形式 (YYYY-MM-DDTHH:mm:ss)
}
```

### PreReadingQuestion (読む前の問い)
```javascript
{
  id: string,
  bookId: string,
  questions: [
    {
      id: string,
      question: string,    // テンプレート質問文
      answer: string       // ユーザーの回答
    }
  ],
  createdAt: string
}
```

**デフォルト質問テンプレート**:
1. この本を読んだら、どんなメリットがあるか？
2. どんな新しい知識が得られるだろうか？
3. 自分が抱えているどんな悩みが解決しそうか？
4. 誰かに話せる面白いネタが見つかるだろうか？
5. これを自分の仕事や生活に活かすならどうなるか？

### ChapterNote (章メモ)
```javascript
{
  id: string,
  bookId: string,
  chapterNumber: number,   // 章番号
  chapterTitle: string,    // 章タイトル
  important: string,       // 最も重要なこと
  memory: string,          // 記憶方法（視覚化、既存知識との結びつき、比喩など）
  application: string,     // 応用方法（現実への適用、似た事例）
  createdAt: string
}
```

### ActionItem (実践項目)
```javascript
{
  id: string,
  bookId: string,
  content: string,         // 実践内容
  isDone: boolean,         // 完了フラグ
  doneDate: string | null, // 実践日 (YYYY-MM-DD)
  note: string,            // 実践メモ・結果
  createdAt: string
}
```

---

## 主要機能

### 1. 本の管理機能

#### 1.1 本の登録
- タイトル、著者、カテゴリーの入力
- 自動で読書ステータス「読む前 (before)」を設定
- 登録と同時に「読む前の問い」画面へ遷移

#### 1.2 本の一覧表示
- カード形式でリスト表示
- ステータス別フィルター（全て / 読む前 / 読書中 / 読了）
- カテゴリー別フィルター
- タイトル・著者名での検索機能
- 各カードに表示する情報：
  - タイトル
  - 著者
  - ステータスバッジ
  - 章メモ数、実践項目数

#### 1.3 本の詳細画面
- 基本情報表示・編集
- タブ切り替えUI:
  - 「読む前の問い」タブ
  - 「章メモ」タブ
  - 「実践リスト」タブ

### 2. 読む前の問い機能

#### 2.1 質問テンプレート表示
- 5つの標準質問を自動表示
- テキストエリアで回答入力
- 保存ボタンで回答をLocalStorageに保存

#### 2.2 回答の表示・編集
- 既存の回答がある場合は自動入力
- いつでも編集可能

### 3. 章メモ機能

#### 3.1 章の追加
- 章番号（自動インクリメント or 手動入力）
- 章タイトル
- 3つの入力フィールド:
  - **重要なこと**: この章で最も重要なポイント
  - **記憶方法**: どう覚えるか（視覚化、比喩、既存知識との結びつけ）
  - **応用方法**: 現実にどう活かすか、似た事例

#### 3.2 章の一覧表示
- 章番号順に表示
- 各章のサマリー表示
- タップで詳細表示・編集
- 削除機能

### 4. 実践チェックリスト機能

#### 4.1 実践項目の登録
- 実践内容のテキスト入力
- 自動で未完了状態で保存

#### 4.2 実践の記録
- チェックボックスで完了/未完了を切り替え
- 完了時に実践日を自動記録
- 実践メモ・結果を追記可能

#### 4.3 実践項目の一覧
- 未完了項目を上に表示
- 完了項目はグレーアウト表示
- 削除機能

### 5. 統計・グラフ機能

#### 5.1 読書統計
- 総読書冊数
- 月別読書冊数（当月、過去3ヶ月、過去12ヶ月）
- カテゴリー別読書冊数
- 実践完了率（完了項目数 / 総項目数）

#### 5.2 グラフ表示
- **月別読書数**: 棒グラフ (Chart.js Bar Chart)
- **カテゴリー別**: 円グラフ (Chart.js Pie Chart)
- **実践達成率**: プログレスバー + パーセンテージ表示

---

## UI/UX要件

### デザイン方針
- **シンプル & クリーン**: 読書に集中できる落ち着いた配色
- **直感的な操作**: タップしやすいボタンサイズ (最小44x44px)
- **視覚的フィードバック**: ボタン押下時のアニメーション
- **読みやすさ**: 適切な行間、フォントサイズ

### カラーパレット案
- **プライマリ**: #4A90E2 (ブルー系)
- **セカンダリ**: #7ED321 (グリーン系 - 完了状態)
- **アクセント**: #F5A623 (オレンジ系 - 注意喚起)
- **背景**: #F8F9FA (明るいグレー)
- **テキスト**: #333333 (ダークグレー)

### レスポンシブ対応
- モバイルファースト設計
- ブレークポイント:
  - スマートフォン: ~767px
  - タブレット: 768px~1023px
  - デスクトップ: 1024px~

### ナビゲーション
- **下部固定ナビゲーションバー**:
  - ホーム (読書中の本、今日の実践項目)
  - 本リスト
  - 統計

---

## 画面設計

### 1. ホーム画面
- **ヘッダー**: アプリ名、日付表示
- **読書中の本セクション**:
  - 読書中の本をカード表示（最大3冊）
  - 「もっと見る」で本リストへ遷移
- **今日の実践セクション**:
  - 未完了の実践項目（最大5件）
  - チェックボックスで即完了可能
- **統計サマリー**:
  - 今月の読書数
  - 実践完了率

### 2. 本リスト画面
- **検索バー**: タイトル・著者で検索
- **フィルターボタン**: ステータス、カテゴリー
- **本のカード一覧**: グリッド or リスト表示
- **追加ボタン**: フローティングアクションボタン (FAB)

### 3. 本の詳細画面
- **ヘッダー**: 本のタイトル、著者、ステータス
- **編集ボタン**: 基本情報の編集
- **タブ**:
  - **読む前の問い**: 質問と回答の表示・編集
  - **章メモ**: 章リスト、追加ボタン
  - **実践リスト**: 実践項目リスト、追加ボタン

### 4. 統計画面
- **期間選択**: 今月 / 過去3ヶ月 / 過去12ヶ月
- **月別読書数グラフ**: Chart.js棒グラフ
- **カテゴリー別グラフ**: Chart.js円グラフ
- **実践達成率**: プログレスバーとパーセンテージ

### 5. フォーム画面
- **本の追加/編集フォーム**
- **章メモの追加/編集フォーム**
- **実践項目の追加/編集フォーム**
- バリデーション: 必須項目のチェック
- キャンセル・保存ボタン

---

## 実装の優先順位

### フェーズ1: MVP (Minimum Viable Product)
1. プロジェクトセットアップ
2. 本の登録・一覧表示・詳細表示
3. 読む前の問い機能（5つの質問テンプレート）
4. 章メモ機能（3つの入力フィールド）
5. LocalStorageでのデータ永続化
6. 基本的なレスポンシブデザイン

### フェーズ2: 機能拡張
1. 実践チェックリスト機能
2. 検索・フィルター機能
3. ステータス管理（読む前 → 読書中 → 読了）
4. 統計機能（基本的な数値表示）

### フェーズ3: 完成版
1. Chart.jsによるグラフ表示
2. データのエクスポート/インポート (JSON形式)
3. UI/UXの改善（アニメーション、トランジション）
4. ダークモード対応
5. PWA対応（オフライン動作、ホーム画面追加）

---

## 技術的な実装ガイドライン

### LocalStorage操作
```javascript
// データの保存
const saveData = (key, data) => {
  localStorage.setItem(key, JSON.stringify(data));
};

// データの取得
const getData = (key) => {
  const data = localStorage.getItem(key);
  return data ? JSON.parse(data) : null;
};

// データキー
// - books: 本の配列
// - preReadingQuestions: 問いの配列
// - chapterNotes: 章メモの配列
// - actionItems: 実践項目の配列
```

### UUIDの生成
```javascript
const generateUUID = () => {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, (c) => {
    const r = Math.random() * 16 | 0;
    const v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
};
```

### Chart.js基本設定
```javascript
// 月別読書数グラフ
new Chart(ctx, {
  type: 'bar',
  data: {
    labels: ['1月', '2月', '3月', ...],
    datasets: [{
      label: '読書数',
      data: [3, 5, 2, ...],
      backgroundColor: '#4A90E2'
    }]
  }
});

// カテゴリー別円グラフ
new Chart(ctx, {
  type: 'pie',
  data: {
    labels: ['ビジネス', '自己啓発', '健康', ...],
    datasets: [{
      data: [10, 5, 3, ...],
      backgroundColor: ['#4A90E2', '#7ED321', '#F5A623', ...]
    }]
  }
});
```

---

## テストケース

### 本の管理
- [ ] 本を登録できる
- [ ] 本の一覧が表示される
- [ ] 本の詳細が表示される
- [ ] 本の情報を編集できる
- [ ] 本を削除できる
- [ ] ステータスを変更できる

### 読む前の問い
- [ ] 5つの質問が自動表示される
- [ ] 回答を保存できる
- [ ] 保存した回答が表示される
- [ ] 回答を編集できる

### 章メモ
- [ ] 章を追加できる
- [ ] 3つのフィールドに入力できる
- [ ] 章の一覧が表示される
- [ ] 章を編集できる
- [ ] 章を削除できる

### 実践項目
- [ ] 実践項目を追加できる
- [ ] チェックボックスで完了できる
- [ ] 実践日が自動記録される
- [ ] 実践メモを追記できる
- [ ] 実践項目を削除できる

### 統計
- [ ] 読書数が正しく集計される
- [ ] グラフが正しく表示される
- [ ] 期間選択で表示が変わる

### データ永続化
- [ ] ページをリロードしてもデータが残る
- [ ] 複数のブックを同時に管理できる

---

## 今後の拡張案

- データのクラウド同期（Firebase等）
- ソーシャル機能（読書記録のシェア）
- AIによる読書推薦
- 読書タイマー機能
- 読書メモのMarkdownエクスポート
- Kindle等の外部サービス連携

---

## 参考リンク

- [LocalStorage API - MDN](https://developer.mozilla.org/ja/docs/Web/API/Window/localStorage)
- [Chart.js Documentation](https://www.chartjs.org/docs/latest/)
- [Mobile-First Design Best Practices](https://web.dev/responsive-web-design-basics/)

---

**作成日**: 2026-02-15  
**バージョン**: 1.0.0  
**目的**: DaiGo式アクティブリーディングの実践支援
